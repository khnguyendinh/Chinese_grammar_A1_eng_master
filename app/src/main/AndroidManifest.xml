<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lamstudio.chinese.grammar"
    android:versionCode="3"
    android:versionName="1.0.2">

    <uses-permission android:name="ANDROID.PERMISSION.INTERNET" />
    <uses-permission android:name="ANDROID.PERMISSION.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.PERMISSION.ACCESS_WIFI_STATE" />
    <uses-permission android:name="ANDROID.PERMISSION.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".volley.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.lamstudio.chinese.grammar.activities.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.lamstudio.chinese.grammar.activities.GrammarDetailActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.lamstudio.chinese.grammar.activities.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.lamstudio.chinese.grammar.activities.GrammarListActivity"
            android:label="@string/app_name" />
    </application>

</manifest>
